export declare type GetDb<DB> = () => Promise<DB>;
interface MongoClientInstance<DB> {
    db: (dbName: string) => DB;
    connect: (callback: (err: Error) => void) => void;
}
export interface MongoClient<DB> {
    new (url: string, options: {
        autoReconnect: boolean;
    }): MongoClientInstance<DB>;
}
export default function <DB>(mongoClientClass: MongoClient<DB>, mongoUrl: string, dbName: string): GetDb<DB>;
export {};
